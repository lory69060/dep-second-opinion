---
description: 写 dep-second-opinion 的 GrokBot 每日任务卡时，必须带 TARGETING；G1 后以 follow-up/G2 为主，禁止退回 X 冷推冲量。
alwaysApply: false
---

# dep-SO GrokBot 任务卡

当用户要求「任务卡 / handoff / 给 GrokBot」且项目为 **dep-second-opinion** 时：

1. 先读 [`docs/grokbot-standing.md`](docs/grokbot-standing.md) 与 [`trials/issue-tracker.md`](trials/issue-tracker.md)。
2. **G1 已达标（≥20）后**：TODAY 主目标改为 **G2** —
   - Primary: 为仍 OPEN、未明确 decline 的帖起草 **soft follow-up**（人不发不算发出）
   - Secondary: 新 Issue 草稿 **≤2**，且必须过 TARGETING
   - `replies=0`（X 冷推默认关）；禁止把「回复数」当进度
3. 每一份 TODAY 必须含 TARGETING Prefer/Avoid 要点（或 `per docs/grokbot-standing.md` + 复述 Prefer≥2 / Avoid）。
4. Skip：tracker 全名单 + 已 decline/CLOSED 禁止 follow-up。
5. REPORT 四行用 standing 里 post-G1 格式（scans / followups+issues / x_replies / tomorrow）。
6. 外壳遵循用户规则 `grokbot-handoff.mdc`；整卡英文、短。
